# Copy this file to .env (at the repo root) and paste your real key.
# Notebooks 3 and 5 load ../.env and use this only for the optional
# GPT-4o-mini comparison cells. Leave it unset to skip those sections.
OPENAI_API_KEY=sk-your-key-here
